package com.castify.backend.enums;

public enum MessageType {
    CHAT,           // Regular chat message
    SYSTEM,         // System message (user joined/left)
    PLAYBACK_SYNC,  // Playback synchronization
    EMOJI_REACTION  //
}
